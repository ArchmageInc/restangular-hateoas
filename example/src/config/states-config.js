(function (angular) {
    'use strict';
    angular.module('config')
    .constant('config', {
        states: {
            
        },
        api: {
            baseUrl: 'http://localhost:8080'
        }
    });
}(angular));
